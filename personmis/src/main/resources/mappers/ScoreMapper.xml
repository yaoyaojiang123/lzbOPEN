<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ch.personmis.repository.ScoreMapper">
    <resultMap id="BaseResultMap" type="com.ch.personmis.entity.Score">
        <id column="id" property="id" />
        <result column="student_id" property="studentId" />
        <result column="course_id" property="courseId" />
        <result column="score" property="score" />
        <result column="semester" property="semester" />
        <result column="exam_date" property="examDate" />
        <result column="exam_type" property="examType" />
        <result column="remark" property="remark" />
        <result column="student_name" property="studentName" />
        <result column="student_no" property="studentNo" />
        <result column="course_name" property="courseName" />
        <result column="class_name" property="className" />
    </resultMap>

    <select id="findScoreList" resultMap="BaseResultMap">
        select s.*, st.student_name, st.student_no, c.course_name, ci.class_name 
        from score s
        left join student st on s.student_id = st.id
        left join course c on s.course_id = c.id
        left join class_info ci on st.class_id = ci.id
        <where>
            <if test="studentId != null and studentId != 0">
                and s.student_id = #{studentId}
            </if>
            <if test="courseId != null and courseId != 0">
                and s.course_id = #{courseId}
            </if>
            <if test="studentName != null and studentName != ''">
                and st.student_name like concat('%', #{studentName}, '%')
            </if>
            <if test="courseName != null and courseName != ''">
                and c.course_name like concat('%', #{courseName}, '%')
            </if>
            <if test="semester != null and semester != ''">
                and s.semester = #{semester}
            </if>
            <if test="classId != null and classId != 0">
                and st.class_id = #{classId}
            </if>
        </where>
        order by s.id desc
        limit #{start}, #{limit}
    </select>

    <select id="getScoreCount" resultType="int">
        select count(*) from score s
        left join student st on s.student_id = st.id
        <where>
            <if test="studentId != null and studentId != 0">
                and s.student_id = #{studentId}
            </if>
            <if test="courseId != null and courseId != 0">
                and s.course_id = #{courseId}
            </if>
            <if test="studentName != null and studentName != ''">
                and st.student_name like concat('%', #{studentName}, '%')
            </if>
            <if test="semester != null and semester != ''">
                and s.semester = #{semester}
            </if>
            <if test="classId != null and classId != 0">
                and st.class_id = #{classId}
            </if>
        </where>
    </select>

    <select id="findScoreById" resultMap="BaseResultMap">
        select s.*, st.student_name, st.student_no, c.course_name, ci.class_name 
        from score s
        left join student st on s.student_id = st.id
        left join course c on s.course_id = c.id
        left join class_info ci on st.class_id = ci.id
        where s.id = #{id}
    </select>

    <insert id="addScore" useGeneratedKeys="true" keyProperty="id">
        insert into score(student_id, course_id, score, semester, exam_date, exam_type, remark)
        values(#{studentId}, #{courseId}, #{score}, #{semester}, #{examDate}, #{examType}, #{remark})
    </insert>

    <update id="updateScore">
        update score
        set student_id = #{studentId},
            course_id = #{courseId},
            score = #{score},
            semester = #{semester},
            exam_date = #{examDate},
            exam_type = #{examType},
            remark = #{remark}
        where id = #{id}
    </update>

    <delete id="deleteScore">
        delete from score where id = #{id}
    </delete>

    <select id="findScoresByStudentId" resultMap="BaseResultMap">
        select s.*, c.course_name 
        from score s
        left join course c on s.course_id = c.id
        where s.student_id = #{studentId} order by s.semester
    </select>

    <select id="findScoresByCourseId" resultMap="BaseResultMap">
        select s.*, st.student_name, st.student_no, ci.class_name 
        from score s
        left join student st on s.student_id = st.id
        left join class_info ci on st.class_id = ci.id
        where s.course_id = #{courseId} order by st.student_no
    </select>

    <delete id="batchDeleteScores">
        delete from score where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findScoreByStudentAndCourse" resultMap="BaseResultMap">
        select * from score where student_id = #{studentId} and course_id = #{courseId}
    </select>

    <select id="getScoreStatisticsByClassId" resultType="map">
        select c.course_name, avg(s.score) as avg_score, max(s.score) as max_score, min(s.score) as min_score, count(*) as student_count
        from score s
        left join student st on s.student_id = st.id
        left join course c on s.course_id = c.id
        where st.class_id = #{classId}
        group by c.id, c.course_name
    </select>
</mapper>